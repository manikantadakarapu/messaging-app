import React from 'react';

const Chat = () => {
  return (
    <div style={{ textAlign: 'center', marginTop: '100px' }}>
      <h2>Welcome to the Chat App</h2>
      <p>This is the chat component. We'll build it next!</p>
    </div>
  );
};

export default Chat;
